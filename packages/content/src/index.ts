/**
 * @briefyard/content — JSON-versioned slot corpus + Zod schema + loader.
 *
 * P2-stub state: Zod schemas + CONTENT_VERSION + loader exposed.
 */

export { CONTENT_VERSION } from '../version';
export * from './schema';
export * from './loader';
